---
- name: Windows Update
  hosts: all
  gather_facts: false
  
  tasks:
    - name: Running Windows Update
      ansible.windows.win_updates:
        category_names:
          - SecurityUpdates
      register: result

    - debug: var=result
